# 「我的动漫库」项目进度

## 第 0 轮：需求梳理与项目文档初始化（2026-07-19）

- 已完成：通读 `REQUIREMENTS.md` 全文；建立带章节出处的功能白名单；完整录入第四章和第三十三章的禁止条目及附加禁止规则；确认未增加原始需求之外的功能。
- 修改的关键文件：`FEATURE-BOUNDARIES.md`、`PROGRESS.md`。
- 执行的命令：读取仓库文件列表；读取 `AGENT.md`；使用章节检索并读取 `Project-prompts.md` 的《通用轮次协议》和《第 0 轮》；完整及分段读取 `REQUIREMENTS.md`；使用补丁创建文档；运行 PowerShell 文档覆盖验证脚本，检查产出文件、第四章/第三十三章黑名单覆盖、白名单出处及仓库文件范围。
- 测试结果：通过。`FEATURE-BOUNDARIES.md` 与 `PROGRESS.md` 均存在；第四章共 75 个列表项、遗漏 0；第三十三章共 26 个列表项、遗漏 0；白名单共 49 项、缺少出处 0；仓库共 5 个文件，均为本轮前已有或本轮要求的文档，未生成业务代码。
- 已知问题：无。
- 未完成内容：无（第 0 轮范围内）。
- 下一轮建议：第 0 轮验证通过并经人工确认后，再执行第 1 轮“技术方案与项目骨架”；本轮不提前开始。

## 第 1 轮：技术方案与项目骨架（2026-07-19）

- 已完成：编写并锁定 `ARCHITECTURE.md`；初始化 Next.js App Router、React、TypeScript 严格模式空项目；选定 Drizzle ORM + better-sqlite3、CSS Modules、Zod、TanStack Query；配置 ESLint、类型检查、Vitest 与示例测试；建立需求第 18.3 节目录空壳；创建 `.env.example`、`README.md`、启动/构建脚本和依赖锁文件。首页保持空白，数据库模式保持为空，未实现任何业务功能。
- 修改的关键文件：`ARCHITECTURE.md`、`package.json`、`package-lock.json`、`tsconfig.json`、`next.config.ts`、`eslint.config.mjs`、`vitest.config.ts`、`drizzle.config.ts`、`.env.example`、`.gitignore`、`README.md`、`src/app/layout.tsx`、`src/app/page.tsx`、`src/app/globals.css`、`src/lib/db/schema.ts`、`tests/toolchain.test.ts`，以及第 18.3 节要求目录中的 `.gitkeep`。
- 执行的命令：读取本轮协议、必读需求、第 0 轮进度与仓库状态；核对 Node/npm 版本及 Next.js、Drizzle、Vitest 官方文档；安装并锁定运行时与开发依赖；运行 `npm ls` 和 `npm audit`；将超出配套插件支持范围的 TypeScript 7/ESLint 10 调整为 TypeScript 5.9.3/ESLint 9.39.5；配置同版本官方 SWC WASM 回退；运行 `npm run typecheck`、`npm run lint`、`npm test`、`npm run build`；启动 `npm run dev -- --hostname 127.0.0.1 --port 3100` 并在显示 `Ready` 后由验证超时终止；运行目录、环境变量、空页面、空数据库模式与数据文件范围检查。
- 测试结果：通过。类型检查退出码 0；ESLint 退出码 0；Vitest 1 个测试文件、1 个测试通过；Next.js 16.2.10 生产构建成功，静态生成 `/` 与框架自带 `/_not-found`；开发服务器在 807ms 内进入 `Ready`；22 个要求目录全部存在；5 个环境变量全部存在；无数据库或海报数据文件；首页返回空内容；数据库模式未定义任何表。
- 已知问题：当前 Windows 执行环境无法加载 Next.js 原生 SWC 二进制，已使用同版本官方 `@next/swc-wasm-nodejs` + Webpack 回退，功能验证通过但编译性能低于原生路径。`npm audit` 报告 6 个中等级、0 个高等级、0 个严重等级告警，来自最新版 Next.js 的 PostCSS 传递依赖及最新版 Drizzle Kit 的 esbuild 工具链；审计给出的自动修复会破坏性降级到旧大版本，因此未执行 `npm audit fix --force`。
- 未完成内容：无（第 1 轮范围内）。数据库表、迁移、外部数据源、业务接口和页面内容均按计划留待后续对应轮次。
- 下一轮建议：经人工确认后执行第 2 轮“数据库模型与迁移”；开始前重新读取锁定的 `ARCHITECTURE.md`，不得在第 2 轮重新选择框架、ORM、样式方案或目录架构。

## 第 2 轮：数据库模型与迁移（2026-07-19）

- 已完成：使用 Drizzle 定义 Anime、Franchise、AnimeRelation、SourceReference、AppSetting 五张且仅五张业务表；字段逐项对应 `REQUIREMENTS.md` 第十九章；以 JSON 文本保存 `aliases`；为 `status` 和 `relationType` 建立数据库 `CHECK`；为 Anime 的 `source + sourceId` 和关系记录建立组合唯一约束；建立外键及状态筛选、最近添加、中文标题、年份、系列和关系查找索引；生成并执行首个迁移；更新数据库初始化文档和命令；新增迁移、字段、唯一约束、状态与关系约束测试。
- 修改的关键文件：`src/lib/db/schema.ts`、`drizzle/0000_initial_schema.sql`、`drizzle/meta/0000_snapshot.json`、`drizzle/meta/_journal.json`、`tests/toolchain.test.ts`、`package.json`、`README.md`、`PROGRESS.md`；本地生成并迁移 `data/anime.db`（已由 `.gitignore` 排除）。
- 执行的命令：读取第 2 轮协议、必读需求、锁定架构、功能边界、最新进度和现有数据库空壳；核对 Drizzle 官方 SQLite 约束、列类型与迁移文档；运行 `npm run typecheck`、`npm run lint`；运行 `npm run db:generate` 生成 `0000_initial_schema.sql` 并人工检查 SQL；运行 `npm test` 在内存全新数据库应用迁移并验证约束；确认目标路径与数据库不存在后运行 `npm run db:migrate`；运行 `npm run db:check`；以只读方式检查实库表、空数据、迁移记录、完整性与外键；执行五表数量及第三十三章禁止字段审计；再次运行类型检查、lint、测试和迁移一致性检查。
- 测试结果：通过。Drizzle 识别 5 张表；迁移在内存全新数据库和 `data/anime.db` 均成功执行；Vitest 1 个测试文件、5 项测试全部通过；重复 Anime `source + sourceId`、非法状态、重复关系、非法关系类型和自关联均被数据库拒绝；实库恰有 5 张业务表、各表 0 行、迁移记录 1 条、`PRAGMA integrity_check` 为 `ok`、外键违规 0；迁移一致性检查通过；TypeScript 与 ESLint 退出码均为 0；迁移中 `CREATE TABLE` 恰为 5 个，禁止字段命中 0。
- 已知问题：无数据库模型或迁移问题。第 1 轮记录的 SWC WASM 回退和依赖审计告警未发生变化。SQLite 外键需由后续建立的每个应用数据库连接显式启用；本轮约束测试已启用并验证外键，生产连接尚未进入本轮允许范围。
- 未完成内容：无（第 2 轮范围内）。未创建 API、Repository、外部数据源请求、UI 或业务页面。
- 下一轮建议：经人工确认后执行第 3 轮“本地动漫读取接口”；复用当前 schema 与迁移，不得改表。建立应用数据库连接时必须启用 `foreign_keys`，并保持首页读取仅访问本地 SQLite。

## 第 3 轮：本地动漫读取接口（2026-07-19）

- 已完成：建立启用 SQLite 外键的本地数据库连接、Anime Repository 与只读 Service；实现 `GET /api/anime`，支持 `ALL/WATCHING/COMPLETED` 状态筛选、默认最近添加排序、中文标题排序、年份排序且缺失年份置后，以及中文标题、原始标题、英文标题、别名本地搜索；返回真实全部/在看/已看完计数；实现 `GET /api/anime/:id` 与 404；使用 Zod 严格校验所有查询参数和正整数 ID；统一 `{ data, error }` 成功/失败响应；补充读取 API 文档与集成测试。未修改数据库 schema 或迁移。
- 修改的关键文件：`src/lib/db/client.ts`、`src/lib/validation/anime-query.ts`、`src/server/repositories/anime-repository.ts`、`src/server/services/anime-read-service.ts`、`src/server/http/api-response.ts`、`src/server/http/anime-handlers.ts`、`src/app/api/anime/route.ts`、`src/app/api/anime/[id]/route.ts`、`src/types/api.ts`、`src/types/anime.ts`、`tests/anime-read-api.test.ts`、`vitest.config.ts`、`package.json`、`next.config.ts`、`ARCHITECTURE.md`、`README.md`、`PROGRESS.md`。
- 执行的命令：读取第 3 轮协议、必读需求、锁定架构、最新进度、schema 和迁移；核对 Next.js Route Handler 与动态参数文档、Drizzle 查询/筛选/排序/计数文档；运行类型检查、lint 和测试并只修复本轮类型问题；运行 `next typegen` 发现并修复路由文件额外导出问题；运行 `npm run build`；启动构建后的生产服务并真实请求空库列表、缺失详情和非法参数；运行 `npm run db:check`；执行写入处理器、外部 URL 和禁止字段范围审计；再次运行类型检查、lint 和全部测试。
- 测试结果：通过。Next 类型生成与 TypeScript 检查退出码 0；ESLint 退出码 0；Vitest 2 个测试文件、18 项测试全部通过，覆盖空库、真实计数、状态筛选、最近添加/中文标题/年份三种排序、四个本地搜索字段、详情成功、404、非法 ID、非法及重复查询参数；生产构建成功并生成动态 `/api/anime`、`/api/anime/[id]`；真实生产服务对空库列表返回 200 与 0/0/0 计数，对不存在 ID 返回 404，对非法状态返回 400，响应格式一致；迁移一致性通过；API 路由文件 2 个、写入处理器 0、外部 URL 字面量 0、禁止字段命中 0。
- 已知问题：无本地读取接口问题。当前环境的原生 SWC 缺陷会使 Next WASM 内置类型检查工作进程崩溃；已将构建改为先强制执行 `next typegen + tsc --noEmit`，通过后才跳过 Next 的重复内置检查，并将同一 WASM 回退补充到 `start`。影响仅限工具链，API 与运行时不变；恢复原生 SWC 时应移除该回退并重新验证。第 1 轮依赖审计告警未变化。
- 未完成内容：无（第 3 轮范围内）。未实现写入接口、外部数据源、导入流程或任何 UI。
- 下一轮建议：经人工确认后执行第 4 轮“Bangumi 适配器”；只接入 Bangumi 一个数据源，不修改当前读取接口、数据库结构或 UI。

## 第 4 轮：数据源一：Bangumi 适配器（2026-07-19）

- 已完成：阅读 Bangumi 当前官方 OpenAPI 与 User-Agent 规范；仅接入 Bangumi v0 数据源；实现统一 `searchAnime`、`getAnimeDetail`、`getAnimeRelations`、`getPosterCandidates` 四个方法；搜索固定筛选动画条目与非 NSFW 内容；按需求 9.1 归一化 13 个字段，缺失字段保持 `null`，未把未标注的拉丁字母别名猜作英文标题；只保留动画关系并保留 Bangumi 原始关系名称；实现 10 秒超时、`TIMEOUT/RATE_LIMIT/UNAVAILABLE` 类型化错误、JSON Content-Type/响应结构校验；实现搜索 5 分钟、详情与关系 30 分钟、最多 100 项且可清理的内存 TTL 缓存；令牌仅由服务端 `BANGUMI_API_TOKEN` 环境变量工厂读取；按官方要求增加 `BANGUMI_USER_AGENT` 配置。未修改数据库、迁移、现有读取 API 或 UI。
- 修改的关键文件：`src/lib/sources/types.ts`、`src/lib/sources/errors.ts`、`src/lib/sources/ttl-cache.ts`、`src/lib/sources/bangumi/schemas.ts`、`src/lib/sources/bangumi/adapter.ts`、`src/lib/sources/bangumi/index.ts`、`src/lib/sources/normalize/bangumi.ts`、`tests/bangumi-adapter.test.ts`、`tests/fixtures/bangumi/*`、`.env.example`、`README.md`、`PROGRESS.md`。
- 执行的命令：读取本轮协议、必读需求、锁定架构、最新进度和现有代码；读取 Bangumi 官方 `dist.json` OpenAPI 与 User-Agent 规范；确认当前端点为 `POST /v0/search/subjects`、`GET /v0/subjects/{subject_id}`、`GET /v0/subjects/{subject_id}/subjects`；以符合规范的 User-Agent 调用公开 API，成功取得条目 55770 的搜索、详情和关系真实响应并裁剪为仅含适配器消费字段的开发测试 fixture；运行 `npm run typecheck`、`npm run lint`、`npm test`、`npm run build`；执行环境令牌、Bearer 字面量、禁用字段、其他数据源、API 路由、数据库表和迁移范围审计。
- 测试结果：通过。TypeScript 检查退出码 0；ESLint 退出码 0 且无警告；Vitest 3 个测试文件、28 项测试全部通过，其中本轮新增 10 项，覆盖四个统一方法、真实响应归一化、无依据字段不补全、请求路径/方法/筛选/请求头、封面顺序、详情缓存复用、搜索 TTL 过期、空搜索、限流不缓存、不可用、响应结构错误、超时及环境令牌读取；Next.js 生产构建成功。审计结果：适配器源码禁用字段命中 0、非空令牌赋值 0、长 Bearer 字面量 0、AniList/TMDB 新文件 0；数据库仍为 5 张表和 1 个迁移，API 路由仍为 2 个。公开 API 的无令牌样本请求成功。
- 已知问题：未验证：真实 API 调用（带 Token）。当前进程未配置 `BANGUMI_API_TOKEN`，因此没有伪造带令牌验证结果；公开 API 调用和基于其响应的离线适配器测试均已完成。第 1 轮记录的 SWC WASM 回退和 6 个中等级依赖审计告警未发生变化。
- 未完成内容：无（第 4 轮范围内）。未实现外部搜索路由、导入或数据库写入，未接入 AniList/TMDB，未创建任何 UI。
- 下一轮建议：经人工确认后执行第 5 轮“外部搜索与导入流程（后端）”；复用已验证的 Bangumi 适配器，不修改其统一结构，不提前接入其他数据源。

## 第 5 轮：外部搜索与导入流程（后端）（2026-07-19）

- 已完成：实现动态 `GET /api/search`，严格校验唯一且非空的 `query`，仅调用 Bangumi 适配器并返回需求 9.1 的标准化条目及基于本地数据库的真实 `isImported`；实现动态 `POST /api/anime/import`，支持 1 至 100 项批量导入、顶层统一状态、逐项状态覆盖及默认 `WATCHING`。每项严格按 8.6 顺序完成完整资料、候选海报、明确关系、标准化、查重、防重复、海报落盘、事务保存和真实结果返回；单项失败不会影响批量其他项。默认海报按适配器质量顺序尝试，校验响应状态、JPEG/PNG/WebP Content-Type 与 1 字节至 10 MiB 大小后写入 `POSTER_STORAGE_PATH/default/`，数据库保存受控相对路径；数据库写入失败时清理刚下载的文件。Anime、SourceReference、Franchise 与当前已落库关联条目的 AnimeRelation 在单条事务中保存；Franchise 只依据 Bangumi 明确动画关系建立/合并，关系类型只按明确关系名称映射，未知关系保存为 `OTHER`，不检查或猜测标题。未修改 schema 或迁移。
- 修改的关键文件：`src/app/api/search/route.ts`、`src/app/api/anime/import/route.ts`、`src/lib/images/default-poster-storage.ts`、`src/lib/validation/external-api.ts`、`src/server/http/external-handlers.ts`、`src/server/http/source-error-response.ts`、`src/server/repositories/anime-import-repository.ts`、`src/server/services/external-search-service.ts`、`src/server/services/anime-import-service.ts`、`src/types/api.ts`、`src/types/external.ts`、`tests/anime-import-api.test.ts`、`README.md`、`PROGRESS.md`。
- 执行的命令：读取 `AGENT.md`、通用轮次协议、第 5 轮、需求 8.2/8.5/8.6、9.3、10.1、19.1 至 19.4、第二十章、锁定架构、最新进度、现有数据库/适配器/API 代码和最近测试结果；核对 Next.js 当前 Route Handler 官方文档及 Drizzle 当前事务、参数化插入、冲突处理官方文档；运行 `npm run typecheck`、`npm run lint`、`npm test`、`npm run db:check`、`npm run build`；执行禁用字段、密钥、Bearer 字面量、TODO、其他数据源、路由数量、表/迁移数量、真实数据库行数和真实海报目录污染审计。
- 测试结果：通过。Next 类型生成与 TypeScript 检查退出码 0；ESLint 退出码 0 且无警告；Vitest 4 个测试文件、35 项测试全部通过，其中本轮新增 7 项集成测试，使用内存 SQLite、mock Bangumi 与 mock 图片响应、独立临时海报目录，覆盖成功批量导入、默认/逐项状态、真实文件字节落盘、SourceReference/Franchise/AnimeRelation 写入、重复导入阻止且不重复下载、批量单条失败隔离、缺失可空字段保存为 null 且 aliases 保存空数组、搜索标准化与真实导入标记、非法输入短路、缺少 Bangumi 配置时的真实路由状态；迁移一致性通过；生产构建成功并生成 `/api/search` 与 `/api/anime/import` 两个动态路由。审计结果：本轮生产文件禁用字段命中 0、非空令牌赋值 0、长 Bearer 字面量 0、TODO/FIXME 0、AniList/TMDB 新文件 0；数据库仍为 5 张表和 1 个迁移；真实五张业务表全部 0 行，真实海报新增文件 0，测试未污染本地数据。
- 已知问题：无第 5 轮后端搜索、导入、事务或海报落盘问题。第 4 轮记录的“未验证：真实 API 调用（带 Token）”仍因当前进程未配置 `BANGUMI_API_TOKEN` 而保持不变；第 1 轮记录的 SWC WASM 回退和 6 个中等级依赖审计告警也未变化。
- 未完成内容：无（第 5 轮范围内）。未实现任何前端界面、自定义封面、状态修改或删除；未接入 AniList/TMDB；未依据标题补充季度、OVA/OAD 或作品关系。
- 下一轮建议：经人工确认后执行第 6 轮“首页海报墙（最小可用版）”；只读取现有本地 API，不在首页重新请求外部数据源，也不提前实现搜索导入 UI。

## 第 6 轮：首页海报墙（最小可用版）（2026-07-19）

- 已完成：实现读取 `GET /api/anime` 真实数据的首页海报墙；建立 `AppSidebar`、`TopBar`、`LocalSearchInput`、`StatusTabs`、`SortSelect`、`AnimeGrid`、`AnimeCard`、`AnimePoster`、`AnimeStatusText`、`EmptyLibraryState` 组件并以 TanStack Query 统一请求本地 API。侧栏和状态标签均支持 `ALL/WATCHING/COMPLETED` 筛选，排序支持 `RECENT/TITLE/YEAR`，本地搜索使用 500 ms 防抖并把查询传给现有只读 API；全部/在看/已看完计数完全采用数据库响应，空库显示 0/0/0。海报固定 2:3、`object-fit: cover`、懒加载，按本地默认海报、已保存远程地址、受控中性占位图的顺序回退；新增只读受控路由 `/api/posters/default/[filename]` 以展示第 5 轮已经落盘的默认海报并设置浏览器长期缓存。卡片只显示海报、标题和状态；界面采用需求锁定的暖灰色板、克制边框和轻微悬停反馈，没有评分、集数、观看进度、简介、菜单、详情弹窗、导入界面或动画润色。
- 修改的关键文件：`src/app/page.tsx`、`src/app/layout.tsx`、`src/app/globals.css`、`src/app/api/posters/default/[filename]/route.ts`、`src/components/common/QueryProvider.tsx`、`src/components/layout/AppSidebar.tsx`、`src/components/layout/TopBar.tsx`、`src/components/anime/AnimeLibraryHome.tsx`、`src/components/anime/LocalSearchInput.tsx`、`src/components/anime/StatusTabs.tsx`、`src/components/anime/SortSelect.tsx`、`src/components/anime/AnimeGrid.tsx`、`src/components/anime/AnimeCard.tsx`、`src/components/anime/AnimePoster.tsx`、`src/components/anime/AnimeStatusText.tsx`、`src/components/anime/EmptyLibraryState.tsx`、对应 CSS Modules、`public/placeholders/anime-poster.svg`、`tests/home-library-ui.test.ts`、`README.md`、`PROGRESS.md`。
- 执行的命令：阅读 `AGENT.md`、`Project-prompts.md` 的《通用轮次协议》与《第 6 轮》、相关需求章节、锁定架构和最新进度；核对 Next.js Server/Client Components、SPA/React Query 与 TanStack Query `useQuery` 当前官方文档；运行 `npm run typecheck`、`npm run lint`、`npm test`、`npm run build`；在项目内创建并迁移隔离临时数据库，先核验空库，再装入 4 条来自既有 Bangumi 真实响应夹具的临时数据，使用本机页面逐项检查真实计数、海报显示、状态筛选、排序、本地搜索、禁用按钮、空库/有数据视觉状态和控制台错误；停止临时服务并删除隔离数据库；审计正式数据库五张业务表、正式海报目录、禁用文案、渐变/阴影及第 6 轮范围。
- 测试结果：通过。Next 类型生成与 TypeScript 检查退出码 0；ESLint 退出码 0 且无警告；Vitest 5 个测试文件、43 项测试全部通过，其中本轮新增 8 项组件/路由测试，覆盖真实计数及 0 计数、状态切换、三种排序选项、查询参数生成、卡片字段边界、懒加载和本地海报优先、精确空库文案、受控海报读取与浏览器缓存、路径越界拒绝和占位图回退；生产构建成功并生成静态首页及动态海报读取路由。人工页面核验中，空库显示 0/0/0 与规定文案；隔离有数据状态显示 4/2/2，四张真实海报均为 2:3 `cover` 且 `loading=lazy`，已看完筛选显示 2 条，年份排序按现有 API 顺序返回，本地搜索 `LOST` 仅显示 1 条，侧栏筛选同步生效，控制台无错误或警告。正式数据库五张业务表仍全部 0 行，正式海报目录只有两个既有 `.gitkeep`，测试没有污染实际数据。
- 已知问题：第 4 轮记录的“未验证：真实 API 调用（带 Token）”仍因当前进程未配置 `BANGUMI_API_TOKEN` 而保持不变；第 1 轮记录的 SWC WASM 回退和 6 个中等级依赖审计警告也未变化。当前环境启动本机页面核验服务需要额外本地执行权限，但不影响构建产物或应用逻辑。
- 未完成内容：第 6 轮允许“添加动漫”按钮暂不接线，因此顶部和空库中的添加按钮当前均明确禁用；该入口与外部搜索/导入界面留到第 7 轮。没有实现详情弹窗、编辑、删除、自定义封面、关系展示、分页、拖拽或任何第 6 轮之外功能。
- 下一轮建议：经人工确认后执行第 7 轮“搜索并添加动漫界面”；复用现有 `/api/search` 与 `/api/anime/import`，为当前禁用的添加入口接线，但不提前实现详情、编辑、删除或自定义封面。

## 第 7 轮：搜索导入界面（前端）（2026-07-19）

- 已完成：启用首页顶部和空库中的“添加动漫”入口，建立不跳转后台页面的全屏 `SearchImportModal`；实现 `ExternalSearchInput` 及 500ms 防抖自动搜索，同时保留明确的搜索按钮，所有结果只来自现有 `/api/search`。实现 `SearchResultGroup`、`SearchResultCard`、`ImportSelectionBar`、`SearchEmptyState`：结果仅按数据源明确的 `mediaType` 保守分入 OVA/OAD、剧场版、特别篇、总集篇、外传/衍生或正传，无法确认的 TV/WEB 等条目统一进入“其他搜索结果”，不读取标题猜测；卡片仅显示小型海报、中文标题、原始标题、年份、类型、总集数、Bangumi 来源、真实已添加状态、选择框与选中后的单项状态。支持多选/取消选择但不默认全选、不自动导入相关作品；默认“在看”，支持统一设置和逐项覆盖“在看/已看完”。批量请求复用 `/api/anime/import`，导入期间只显示真实“正在导入……”状态，完成后显示真实成功/失败数量并逐项列出失败原因；成功条目从选择中移除，失败条目保留以便重试，同时失效并刷新外部搜索缓存与首页本地列表缓存，使新海报和侧栏计数自动更新。桌面端为大型全屏对话框，390px 移动端为无横向溢出的近全屏纵向布局；使用暖灰实色平面、克制边框和需求允许的轻微状态反馈，没有加入详情、编辑、删除、多数据源、假结果或视觉精修。
- 修改的关键文件：`src/components/search/SearchImportModal.tsx`、`src/components/search/ExternalSearchInput.tsx`、`src/components/search/SearchResultGroup.tsx`、`src/components/search/SearchResultCard.tsx`、`src/components/search/ImportSelectionBar.tsx`、`src/components/search/SearchEmptyState.tsx`、`src/components/search/search-import-model.ts`、`src/components/search/debounce.ts`、对应 CSS Modules、`src/components/anime/AnimeLibraryHome.tsx`、`src/components/anime/EmptyLibraryState.tsx`、`src/components/layout/TopBar.tsx`、对应入口样式、`tests/search-import-ui.test.ts`、`tests/anime-import-api.test.ts`、`tests/home-library-ui.test.ts`、`README.md`、`PROGRESS.md`。
- 执行的命令：阅读 `AGENT.md`、`Project-prompts.md` 的《通用轮次协议》与《第 7 轮》、需求 8.1 至 8.6、第十七章、第二十四章、第二十六章及关键交互流程、锁定架构、最新进度和现有首页/搜索/导入代码；使用 `minimalist-ui` 规范约束暖灰平面、留白和控件样式，并按轮次协议排除其中的渐变、滚动动画与提前视觉精修；运行 `npm run typecheck`、`npm run lint`、`npm test`、`npm run build`；启动本机开发服务，以正式空数据库人工检查顶部/空库双入口、桌面模态、搜索等待态、配置缺失失败态、重试入口、关闭行为、控制台日志及 390×844 移动端布局；发现并修复模态主体背景变量错误后重新截图和几何核验；停止临时服务；审计正式数据库、正式海报、迁移/schema 数量、直接外部请求、多数据源、禁用字段、标题猜测、假结果、TODO/FIXME 与临时文件。
- 测试结果：通过。Next 类型生成与 TypeScript 检查退出码 0；ESLint 退出码 0 且无警告；Vitest 6 个测试文件、52 项测试全部通过，其中本轮新增 9 项测试：8 项前端组件/模型测试覆盖 500ms 防抖只提交最后输入、外部搜索 URL、仅按明确来源类型分组且不从标题猜测、8.4 允许字段与禁止字段、批量统一/逐项状态请求、真实导入中及部分失败文案、规定无结果/失败文案、两个添加入口启用；1 项闭环集成测试在内存 SQLite、mock Bangumi 和独立临时海报目录中真实执行“搜索进击的巨人 → 多选两个条目 → 设置不同状态 → 批量导入 → 本地首页读取”，得到成功 2、失败 0、首页计数 2/1/1，两个本地海报路径均存在。生产构建成功。人工核验中两个入口均打开同一模态，输入后立即显示“正在搜索……”，500ms 后请求；当前无 `BANGUMI_USER_AGENT` 的运行环境正确显示“暂时无法从该数据源获取结果。”与“重试”，控制台错误/警告 0；移动端对话框为 390×844、文档宽度 390，无横向溢出。正式五张业务表仍全部 0 行，正式海报新增文件 0，数据库仍为 5 张表和 1 个迁移，测试与人工核验均未污染实际数据。
- 已知问题：未验证：在浏览器中使用真实 Bangumi User-Agent 执行公开搜索并把真实条目写入正式数据库。当前工作区未配置 `.env`/`.env.local`，为避免伪造开发者标识或用验证数据污染实际收藏，本轮没有临时冒充 User-Agent，也没有向正式数据库导入条目；真实 Bangumi 适配器和公开 API 已在第 4 轮验证，搜索/导入后端已在第 5 轮验证，本轮以同一接口契约、内存数据库和隔离海报目录完成全闭环集成测试。第 1 轮记录的 SWC WASM 回退和 6 个中等级依赖审计警告保持不变。
- 未完成内容：无（第 7 轮范围内）。详情模态、点击海报查看详情、状态修改、删除、自定义封面、相关作品交互、AniList/TMDB、多源去重、上传和视觉精修均未提前实现。
- 下一轮建议：经人工确认后执行第 8 轮“详情模态窗口与状态切换”；复用当前首页缓存键和第 7 轮导入能力，但不提前实现第 9 轮删除或第 12 轮封面管理。里程碑：第 7 轮最小可运行闭环“搜索 → 勾选 → 导入 → 首页出现海报”已通过隔离集成测试完成。

## 第 8 轮：详情模态窗口与状态切换（2026-07-19）

- 已完成：把首页动漫卡片改为完整的原生按钮，点击后在首页上方打开居中的详情模态窗口；实现半透明遮罩、右上角关闭、ESC/遮罩关闭、弹窗内部滚动、背景滚动锁定、打开时焦点进入关闭按钮、正反向 Tab 焦点循环及关闭后焦点返回原卡片（原卡片因筛选消失时回退到首页主区域）。详情严格只展示第 7.1 节允许的海报、中文/原始标题、年份、类型、总集数、制作公司、简介、所属系列、观看状态和相关作品，缺失值统一显示“暂无资料”；“更换封面”和“删除”以禁用态保留，分别注明第 12 轮和第 9 轮启用。扩展 `GET /api/anime/:id`，按需合并本地系列/双向关系和 Bangumi 明确关系，外部数据源不可用时仍返回已有本地详情；相关作品只按明确关系分组，不依据标题猜测，并标注已添加/未添加。点击已添加作品会在当前模态内切换详情；点击未添加作品先打开导入确认并复用第 7 轮导入能力，不会自动导入。新增 `PATCH /api/anime/:id/status` 严格校验 `WATCHING/COMPLETED`，状态保存后显示短暂“已保存”，同步刷新首页卡片与全部/在看/已看完计数；在“在看”筛选中切换为“已看完”后卡片即时消失。里程碑：搜索导入、查看收藏、标记状态三大核心用途均已真实可用。
- 修改的关键文件：`src/app/api/anime/[id]/route.ts`、`src/app/api/anime/[id]/status/route.ts`、`src/server/repositories/anime-repository.ts`、`src/server/services/anime-detail-service.ts`、`src/server/services/anime-status-service.ts`、`src/server/http/anime-handlers.ts`、`src/lib/validation/anime-query.ts`、`src/types/anime.ts`、`src/types/api.ts`、`src/components/anime/AnimeDetailModal.tsx`、`src/components/anime/AnimeDetailModal.module.css`、`src/components/anime/AnimeMetadata.tsx`、`src/components/anime/AnimeStatusSelector.tsx`、`src/components/anime/RelatedAnimeList.tsx`、`src/components/anime/AnimeLibraryHome.tsx`、`src/components/anime/AnimeGrid.tsx`、`src/components/anime/AnimeCard.tsx`、`src/components/anime/AnimeCard.module.css`、`tests/anime-detail-status.test.ts`、`tests/anime-detail-ui.test.ts`、`tests/home-library-ui.test.ts`、`README.md`、`PROGRESS.md`。
- 执行的命令：阅读 `AGENT.md`、`Project-prompts.md` 的《通用轮次协议》与《第 8 轮》、指定需求章节、完整 `ARCHITECTURE.md`、最新进度及现有详情/首页/导入实现；使用 `minimalist-ui` 规范约束暖灰实色、平面边框和克制层级，并排除渐变、重阴影和提前视觉精修；运行 `npm run lint`、`npm run typecheck`、`npm test`、`npm run build`。复制并迁移正式空库得到隔离临时数据库，装入两条本地关联作品后以独立端口启动开发服务；使用应用内浏览器在 1280×720 桌面视口检查弹窗尺寸、居中、遮罩、内部滚动、背景锁定、字段边界、禁用按钮、关联分组、键盘焦点循环、ESC 关闭、焦点归还、当前弹窗关联切换和状态更新；只读查询临时库确认状态持久化，再停止服务并删除临时数据库、脚本与日志。
- 测试结果：通过。Next 类型生成与 TypeScript 严格检查退出码 0；ESLint 退出码 0 且无警告；Vitest 8 个测试文件、60 项测试全部通过。新增测试覆盖允许详情字段与禁用字段边界、缺失值回退、系列和已导入/未导入关系合并、外部数据源不可用时的本地关系回退、明确关系分组且不按标题猜测、未导入作品导入确认、状态 PATCH 校验/404/持久化、计数更新及“在看”列表移除。Next.js 16.2.10 生产构建成功，生成动态 `/api/anime/[id]/status` 路由。人工验收中弹窗宽 1040px、高度不超过视口 88%，正文在窗口内滚动且页面背景锁定；初始焦点为关闭按钮，Shift+Tab/Tab 能在首尾循环，关联作品在当前窗口切换后按 ESC 关闭并返回原卡片；状态切换后临时库记录为 `COMPLETED`，首页计数从 2/1/1 即时更新为 2/0/2，“在看”列表卡片数变为 0。正式 `data/anime.db` 的 Anime 表在验收前后均为 0 行，测试和人工验收未污染实际收藏。
- 已知问题：当前工作区仍未配置真实 `BANGUMI_USER_AGENT`，因此人工浏览器验收只使用隔离库中的本地明确关系，并显示“部分资料暂时无法加载”；外部关系合并和未导入条目确认流程已由 mock Bangumi 的集成/组件测试验证，未向正式数据库导入外部条目。第 1 轮记录的 SWC WASM 编译回退和 6 个中等级依赖审计警告保持不变。
- 未完成内容：无（第 8 轮范围内）。“删除”仍为禁用态，按计划留到第 9 轮；“更换封面”仍为禁用态，按计划留到第 12 轮。未实现独立详情页、底部展开、左右分屏、抽屉面板、评分、人气、排名、声优/角色列表、每集信息或任何第 8 轮之外功能。
- 下一轮建议：经人工确认后再执行第 9 轮“删除条目”；该轮涉及数据和文件删除，须重新读取其高风险协议并单独验证，不在本轮提前实现。

## 第 9 轮：删除条目（2026-07-19）

- 已完成：启用详情窗口中的“删除”按钮并新增 `ConfirmDialog`，确认文案严格为“确定要从动漫库中删除《作品名称》吗？”，补充只影响本地收藏和本地自定义封面的范围说明；确认框支持遮罩/ESC 取消、初始焦点进入“取消”、正反向 Tab 循环、取消后焦点返回删除按钮，提交期间禁用操作并显示真实“正在删除……”。新增 `DELETE /api/anime/:id` 和删除服务：正整数 ID 校验、404 及统一响应格式保持与现有接口一致；数据库事务显式清理该条目的所有正向/反向 AnimeRelation、SourceReference 和 Anime 主记录，不修改 Franchise、其他 Anime 或外部数据源。删除前只读取数据库中该条目的 `customPosterPath`，要求路径为相对路径、位于 `POSTER_STORAGE_PATH/custom/` 内、没有 `..` 路径段、扩展名为 JPG/JPEG/PNG/WebP、目标为普通文件而非符号链接，并以真实路径再次校验没有逃逸；不安全路径返回 `UNSAFE_CUSTOM_POSTER_PATH` 并在任何数据库/文件删除前拒绝整个请求。若其他 Anime 仍引用同一真实文件则保留文件；安全且不再使用时只删除该自定义封面。默认封面没有进入删除服务。删除成功后失效详情缓存、刷新首页全部筛选缓存并关闭模态，使卡片和全部/在看/已看完计数即时更新。
- 修改的关键文件：`src/app/api/anime/[id]/route.ts`、`src/server/http/anime-handlers.ts`、`src/server/repositories/anime-repository.ts`、`src/server/services/anime-delete-service.ts`、`src/lib/images/custom-poster-storage.ts`、`src/types/anime.ts`、`src/types/api.ts`、`src/components/anime/AnimeDetailModal.tsx`、`src/components/anime/AnimeDetailModal.module.css`、`src/components/modal/ConfirmDialog.tsx`、`src/components/modal/ConfirmDialog.module.css`、`tests/anime-delete.test.ts`、`tests/anime-detail-ui.test.ts`、`README.md`、`PROGRESS.md`。
- 执行的命令：重新阅读 `AGENT.md`、`Project-prompts.md` 的《通用轮次协议》与高风险《第 9 轮》、REQUIREMENTS.md 第 7.4 节与第二十章、完整锁定架构、最新进度及现有详情/仓储/封面代码；使用 `minimalist-ui` 约束确认框延续既有暖灰实色、平面边框和克制危险色，不采用其与本轮边界冲突的渐变、动画或视觉精修建议。运行 `npm run lint`、`npm run typecheck`、`npm test`、`npm run build`。正式空库只读核对后复制为隔离数据库，在隔离海报根目录创建单一测试自定义封面并装入两条 Anime、一条关系和一条来源引用，以独立端口启动开发服务；使用应用内浏览器检查确认框文案、视觉层级、背景锁定、焦点进入/返回、ESC 取消和真实确认删除流程；只读查询隔离库与文件系统核对删除范围，然后停止服务，在解析并确认绝对目标为 `D:\Anime\.tmp\round9-*` 后删除隔离数据库和海报目录；再次审计正式数据库、正式海报目录、临时文件及删除服务中的默认封面引用。
- 测试结果：通过。Next 类型生成与 TypeScript 严格检查退出码 0；ESLint 退出码 0；Vitest 9 个测试文件、65 项测试全部通过。新增 5 项测试：4 项删除集成测试覆盖目标 Anime 消失、正反向关系清理、来源引用清理、无关关系/引用/Anime 保留、自定义封面删除、默认封面和其他自定义文件保留、共享自定义封面保留、路径穿越在删除前以 409 拒绝且记录/外部文件不变、无效 ID 与 404；1 项确认框组件测试覆盖精确文案和提交禁用态。生产构建成功，`/api/anime/[id]` 动态路由同时承载 GET 与 DELETE。人工隔离验收中确认框初始焦点为“取消”，按 ESC 后详情仍打开且焦点返回“删除”；确认删除后弹窗全部关闭，目标卡片消失，首页计数由 2/1/1 更新为 1/0/1，仅保留“星海纪行 第二季”。隔离库目标 Anime、涉及目标的 AnimeRelation、目标 SourceReference 均变为 0，测试自定义封面由存在变为不存在。正式 `data/anime.db` 的 Anime、AnimeRelation、SourceReference 全程均为 0 行；正式海报目录仍只有两个既有 `.gitkeep`；第 9 轮临时产物为 0，未污染或误删实际数据。
- 已知问题：第 12 轮尚未实现自定义封面上传，因此本轮浏览器验收使用隔离库和隔离自定义封面文件验证清理能力；正式库中没有可删除的用户自定义封面。第 1 轮记录的 SWC WASM 编译回退和 6 个中等级依赖审计警告保持不变。
- 未完成内容：无（第 9 轮范围内）。没有删除默认封面、系列记录、其他收藏或任何外部数据；没有实现批量删除、撤销、回收站、软删除或任何需求外删除方式。“更换封面”仍为禁用态并按计划留到第 12 轮。
- 下一轮建议：经人工确认后再执行第 10 轮“数据源二：AniList 与多源去重”；本轮立即停止，不提前接入 AniList 或修改多源逻辑。

## 第 10 轮：数据源二：AniList 与多源去重（2026-07-19）

- 已完成：按 AniList 当前官方 GraphQL 文档接入第二数据源，新增与既有签名完全一致的 AniList 适配器，`ANILIST_API_URL` 由服务端环境变量提供；实现公开动漫搜索、详情、动画关系与海报候选查询，包含超时、限流、GraphQL 错误、响应结构校验、5/30 分钟缓存和仅保留动画关系节点。Bangumi 与 AniList 搜索使用并行请求并分别返回来源状态；任何单源失败均不影响另一来源结果。新增保守跨源去重：来源引用或明确外部 ID 相同时合并，否则必须由非中文标题、年份、类型和集数共同确认；绝不只凭中文标题合并，证据不足时保留独立结果。合并结果保留全部来源引用，导入时统一写入 `SourceReference`，查重和“已导入”判断覆盖主来源及附加来源。搜索卡片显示全部来源，来源失败区显示对应提示和“重试 <来源>”入口；详情关联、关联导入、默认海报路由和批量导入均兼容 AniList。未接入 TMDB，未修改数据库结构或统一适配器方法签名。
- 修改的关键文件：`.env.example`、`README.md`、`src/lib/sources/types.ts`、`src/lib/sources/anilist/adapter.ts`、`src/lib/sources/anilist/schemas.ts`、`src/lib/sources/anilist/index.ts`、`src/lib/sources/normalize/anilist.ts`、`src/lib/sources/normalize/bangumi.ts`、`src/lib/sources/normalize/deduplicate.ts`、`src/server/services/external-search-service.ts`、`src/server/services/anime-import-service.ts`、`src/server/services/anime-detail-service.ts`、`src/server/repositories/anime-import-repository.ts`、`src/server/repositories/anime-repository.ts`、`src/lib/validation/external-api.ts`、`src/types/external.ts`、`src/components/search/SearchResultCard.tsx`、`src/components/search/SearchResultGroup.tsx`、`src/components/search/SearchImportModal.tsx`、`src/components/search/SearchSourceFailures.tsx`、`src/components/search/SearchSourceFailures.module.css`、`src/components/search/search-import-model.ts`、`src/components/anime/AnimeDetailModal.tsx`、`src/components/anime/AnimePoster.tsx`、`src/app/api/posters/default/[filename]/route.ts`、`tests/anilist-adapter.test.ts`、`tests/multi-source-search.test.ts`、`tests/anime-import-api.test.ts`、`tests/search-import-ui.test.ts`、`tests/bangumi-adapter.test.ts`、`tests/fixtures/anilist/*`、`PROGRESS.md`。
- 执行的命令：读取 `AGENT.md`、`Project-prompts.md` 的《通用轮次协议》与《第 10 轮》、REQUIREMENTS.md 第九章和 8.4、完整 ARCHITECTURE.md、最新进度及现有适配器/搜索/导入/详情/UI/测试代码；查阅 AniList 官方 GraphQL、Media 查询、关系和限流文档；通过 PowerShell 向 `https://graphql.anilist.co` 真实 POST 搜索 `Cowboy Bebop` 与 ID 1 关系查询；运行 `npm run typecheck`、`npm run lint`、定向 Vitest、全量 `npm test`、`npm run build`。复制正式空库为隔离临时库，以 3110 端口启动开发服务并真实请求 `/api/search?query=Cowboy%20Bebop`，随后确认端口关闭、校验并删除仅位于 `.tmp/round10-*` 的临时文件；以只读方式复核正式数据库五张业务表和正式海报目录。
- 测试结果：通过。Next 类型生成与 TypeScript 严格检查退出码 0；ESLint 退出码 0；Vitest 11 个测试文件、78 项测试全部通过。新增覆盖 AniList 查询体与字段标准化、详情/海报缓存、只保留动画关系、429/GraphQL/超时错误、环境工厂、双源确实并行、外部 ID 和复合元数据确定合并、中文标题相同但证据不足时不合并、全部来源引用保留、单源失败隔离及重试入口、合并来源引用持久化、AniList 单源导入和来源展示。Next.js 16.2.10 生产构建成功。真实 AniList 搜索和关系请求均返回 HTTP 200；隔离本地接口返回 HTTP 200、4 条 AniList 真实结果，AniList 状态为 `SUCCESS`，未配置的 Bangumi 状态为 `ERROR` 且错误与结果同时返回。正式 `data/anime.db` 的 `anime`、`franchise`、`anime_relation`、`source_reference`、`app_setting` 均为 0 行；正式海报目录仍只有两个既有 `.gitkeep`，第 10 轮临时文件剩余 0。
- 已知问题：工作区仍未配置真实 `BANGUMI_USER_AGENT`，因此本轮无法在同一次真实本地接口请求中让两个来源同时成功；Bangumi 的真实调用已在第 4 轮验证，本轮则真实验证了 AniList 及“Bangumi 失败、AniList 正常返回”的隔离路径。第 1 轮记录的 SWC WASM 编译回退和 6 个中等级依赖审计警告保持不变。
- 未完成内容：无（第 10 轮范围内）。没有接入 TMDB，没有仅凭中文标题合并，没有添加评分、排名、热度、用户数等禁止字段，也没有修改数据库结构或统一适配器方法签名。
- 下一轮建议：经人工确认后再执行第 11 轮“数据源三：TMDB 补充”；开始前须重新阅读其协议和 TMDB 当前官方文档。本轮立即停止，不提前接入 TMDB。

## 第 11 轮：数据源三：TMDB 补充（2026-07-19）

- 已完成：按 TMDB 当前官方 v3 文档接入第三数据源，新增与既有统一签名一致的 `TmdbAdapter`；`TMDB_API_KEY` 仅从服务端环境变量读取，同时兼容 32 位 v3 API Key 查询认证和 API Read Access Token Bearer 认证。电影搜索使用官方 `/search/movie`，并通过官方 `/genre/movie/list` 动态取得 `Animation` 类型 ID，只保留动画电影，避免真人电影污染动漫搜索；详情使用 `/movie/{id}` 并通过 `append_to_response=external_ids` 同次取得 IMDb / Wikidata 外部 ID。实现中文/原始/英文标题、年份、电影类型、制作公司、简介和 TMDB 图片 URL 标准化；`getPosterCandidates` 提供 original、w780、w500 三档候选，搜索缓存 5 分钟、详情和电影类型缓存 30 分钟，并处理超时、429、非 JSON、非成功状态和响应结构异常。TMDB `getAnimeRelations` 始终返回空数组，不参与动画系列关系判断。三源搜索并行、来源状态、失败隔离、重试入口和全部来源引用持久化沿用第 10 轮逻辑；去重主数据优先级明确锁定为 Bangumi → AniList → TMDB，TMDB 只能补齐缺失资料，不能覆盖 Bangumi / AniList 的动画条目与关系。电影集数为 `null` 与单集动画电影的 `1` 仅视为相容证据，仍须同时满足非中文标题、年份和 `MOVIE` 类型才合并。搜索卡片和搜索窗口文案显示 TMDB，TMDB 单源条目可通过现有导入流程保存，默认海报路由支持 `tmdb-<id>` 文件名。未修改数据库结构或统一适配器方法签名，未实现第 12 轮封面上传功能。
- 修改的关键文件：`README.md`、`src/lib/sources/types.ts`、`src/lib/sources/tmdb/adapter.ts`、`src/lib/sources/tmdb/schemas.ts`、`src/lib/sources/tmdb/index.ts`、`src/lib/sources/normalize/tmdb.ts`、`src/lib/sources/normalize/deduplicate.ts`、`src/server/services/external-search-service.ts`、`src/server/services/anime-import-service.ts`、`src/server/services/anime-detail-service.ts`、`src/components/search/SearchImportModal.tsx`、`src/app/api/posters/default/[filename]/route.ts`、`tests/tmdb-adapter.test.ts`、`tests/multi-source-search.test.ts`、`tests/anime-import-api.test.ts`、`tests/search-import-ui.test.ts`、`tests/home-library-ui.test.ts`、`tests/fixtures/tmdb/search.json`、`tests/fixtures/tmdb/detail.json`、`tests/fixtures/tmdb/README.md`、`PROGRESS.md`。
- 执行的命令：读取 `AGENT.md`、`Project-prompts.md` 的《通用轮次协议》与《第 11 轮》、REQUIREMENTS.md 第九章和 18.2、完整 ARCHITECTURE.md、最新 PROGRESS.md 及现有多源适配器/搜索/去重/导入/详情/海报/UI/测试代码；查阅 TMDB 官方认证、电影搜索、电影详情、附加响应、电影外部 ID、图片基础、电影图片、电影类型列表、错误与限流文档；检查当前进程是否配置 `TMDB_API_KEY` 以及 `.env.local` 是否存在，但不读取或输出任何密钥值；运行定向 Vitest、`npm run typecheck`、`npm run lint`、全量 `npm test`、`npm run build`；只读查询正式数据库五张业务表并检查正式海报目录；检索确认生产 TMDB 代码未暴露 popularity、vote、rating、score 等禁止字段，客户端代码未读取 TMDB 密钥。
- 测试结果：通过。Next 类型生成与 TypeScript 严格检查退出码 0；ESLint 退出码 0 且无警告；Vitest 12 个测试文件、84 项测试全部通过；Next.js 16.2.10 生产构建成功。新增 TMDB 适配器测试覆盖官方 URL 与参数、v3 API Key / Bearer 两种认证、只保留 Animation 电影、禁止字段不进入标准化结果、详情与外部 ID、三档海报候选及详情缓存、关系固定为空、429、无效响应、超时和环境工厂。三源合并测试覆盖任意输入顺序下 Bangumi / AniList 优先于 TMDB、电影集数相容规则、三个来源引用完整保留和三源同时发起搜索；集成测试覆盖三个来源引用写入 `SourceReference`、TMDB 单源导入、`default/tmdb-129.jpg` 落盘路径、TMDB 不创建系列关系、三源缺少配置时分别隔离失败，以及 TMDB 默认海报路由。正式 `data/anime.db` 的 `anime`、`franchise`、`anime_relation`、`source_reference`、`app_setting` 均为 0 行；正式海报目录仍只有两个既有 `.gitkeep`。
- 已知问题：**真实 TMDB 调用未验证**。当前进程未配置 `TMDB_API_KEY`，工作区也不存在 `.env.local`，而 TMDB 官方接口要求应用级认证；因此没有伪造或绕过认证进行真实调用。配置有效密钥后可通过现有 `/api/search` 执行真实三源验收。工作区仍未配置真实 `BANGUMI_USER_AGENT`；第 1 轮记录的 SWC WASM 编译回退和 6 个中等级依赖审计警告保持不变。
- 未完成内容：无（第 11 轮可在当前环境完成的代码与自动化验证范围内）。唯一未验证项为需要用户密钥的真实 TMDB 网络调用，已按协议明确说明。没有让 TMDB 覆盖 Bangumi / AniList 主数据或关系，没有展示真人电影、评分、排名、热度、用户数等禁止内容，没有修改数据库结构，也没有实现封面上传、URL 导入、恢复默认封面或其他第 12 轮功能。
- 下一轮建议：用户配置有效 `TMDB_API_KEY` 后可先补做一次真实三源搜索验收；经人工确认后再执行第 12 轮“封面管理与上传安全”。该轮属于高风险文件上传与安全逻辑，必须重新阅读其专门协议。本轮立即停止，不提前实现第 12 轮。

## 第 12 轮：封面管理与上传安全（2026-07-19）

- 已完成：启用详情窗口“更换封面”入口，新增 `PosterManagerModal` 与 `PosterPreview`；本地上传支持 JPG/JPEG、PNG、WebP 和 10 MiB 上限，选中文件后先显示本地对象 URL 预览，只有明确点击“确认保存”才发送并替换当前自定义封面。新增 URL 导入和恢复默认封面，URL 图片在服务端下载并保存到 `POSTER_STORAGE_PATH/custom/`，数据库不把用户输入的网址当作自定义封面地址。新增自定义封面只读路由，并把全站显示优先级锁定为“自定义本地 → 默认本地 → 已保存远程默认地址 → 占位图”；保存成功后同步更新详情缓存和所有首页列表缓存，再后台重新校验。默认封面路径和默认远程地址在上传、URL 导入与恢复过程中始终保留。
- 安全实现：服务端忽略不可信原始文件名，以 Anime ID、随机 UUID 和由验证后 Content-Type 决定的扩展名生成磁盘文件名；只允许普通文件位于真实 `custom/` 目录，拒绝绝对路径、`..`、非法扩展名、符号链接和真实路径逃逸。上传在解析 multipart 前限制总请求体，在保存前再次限制图片字节数、允许类型和 JPEG/PNG/WebP 文件签名。URL 仅允许无凭据、默认端口的 HTTP/HTTPS；DNS 必须返回且全部为公网地址，拒绝 IPv4/IPv6 本机、私网、链路本地、保留、文档、过渡和组播地址；请求锁定到已验证 IP，同时保留原 Host/TLS 主机名以降低 DNS 重绑定风险；每次重定向重新解析并校验，最多 3 次；限制 10 秒、响应状态、Content-Type、Content-Length 和流式实际字节数，下载后再次验证文件签名。上传文件从不作为代码执行。
- 修改的关键文件：`README.md`、`src/lib/images/custom-poster-storage.ts`、`src/lib/images/poster-image-validation.ts`、`src/lib/images/remote-poster-download.ts`、`src/lib/validation/poster.ts`、`src/server/repositories/anime-repository.ts`、`src/server/services/anime-poster-service.ts`、`src/server/http/anime-handlers.ts`、`src/app/api/anime/[id]/poster/upload/route.ts`、`src/app/api/anime/[id]/poster/url/route.ts`、`src/app/api/anime/[id]/poster/custom/route.ts`、`src/app/api/posters/custom/[filename]/route.ts`、`src/types/api.ts`、`src/types/anime.ts`、`src/components/anime/AnimePoster.tsx`、`src/components/anime/AnimeCard.tsx`、`src/components/anime/AnimeDetailModal.tsx`、`src/components/anime/RelatedAnimeList.tsx`、`src/components/anime/PosterManagerModal.tsx`、`src/components/anime/PosterManagerModal.module.css`、`src/components/anime/PosterPreview.tsx`、`tests/anime-poster-management.test.ts`、`tests/home-library-ui.test.ts`、`PROGRESS.md`。
- 执行的命令：读取 `AGENT.md`、`Project-prompts.md` 的《通用轮次协议》与高风险《第 12 轮》、REQUIREMENTS.md 第十章、第二十章封面接口、第二十一章安全要求、第二十七章流程五、第二十九章测试要求、完整 `ARCHITECTURE.md`、最新进度及现有封面/详情/仓储/删除实现；使用 `minimalist-ui` 规范延续既有暖灰实色、平面边框、无渐变和无重阴影的界面。运行定向 Vitest、`npm run typecheck`、`npm run lint`、全量 `npm test` 和 `npm run build`。创建并迁移隔离数据库和隔离海报根目录，装入一条测试 Anime 与默认本地封面，以独立端口启动开发服务；通过真实 multipart HTTP 请求执行“上传 → 首页读取 → 详情读取 → 恢复默认”，核对安全文件名、文件落盘、默认字段保留、两处数据更新、恢复后数据库置空和自定义文件删除；停止服务后解析并确认目标绝对路径，再删除全部隔离数据库、图片和日志。最后以只读模式复核正式数据库五张业务表。
- 测试结果：通过。Next 类型生成与 TypeScript 严格检查退出码 0；ESLint 退出码 0；Vitest 13 个测试文件、101 项测试全部通过；Next.js 16.2.10 生产构建成功并生成上传、URL 导入、恢复默认和自定义封面读取四个动态路由。本轮新增 17 项封面测试，覆盖四级显示优先级、非法自定义路径不进入 URL、管理弹窗允许类型/预览区域/明确确认与恢复入口、安全随机文件名且原始目录穿越文件名无效、替换只在第二次明确请求后发生、首页/详情即时读取更新、恢复默认并删除旧自定义文件、超大上传、伪造 Content-Type、数据库既有目录穿越路径、127.0.0.1、IPv6 回环、169.254.169.254、192.168.x.x、DNS 混合私网答案、重定向至本机、远程声明超大、远程伪造签名、不支持的远程类型及有效远程图片落盘。隔离真实 HTTP 流程全部判定为 `True`；正式 `data/anime.db` 的 `anime`、`anime_relation`、`app_setting`、`franchise`、`source_reference` 均为 0 行，测试未污染实际收藏。
- 已知问题：应用内浏览器对本轮隔离服务的 `http://localhost:3120` 触发 URL 安全策略拦截，因此没有完成本轮弹窗的人工视觉与文件选择交互验收，也没有尝试绕过；组件静态渲染测试覆盖允许类型、预览容器、明确确认按钮初始禁用和恢复入口，真实隔离 HTTP 流程覆盖确认后的服务端闭环。第 11 轮记录的“真实 TMDB 调用未验证”、未配置真实 `BANGUMI_USER_AGENT`、第 1 轮的 SWC WASM 编译回退和 6 个中等级依赖审计警告均未变化。
- 未完成内容：无（第 12 轮代码、安全校验与自动化/隔离服务验证范围内）。没有自动覆盖原封面、没有信任原始文件名、没有把 URL 直接存为自定义封面、没有删除数据源默认封面，也没有实现裁剪、旋转、滤镜、压缩设置、图库、拖拽上传或任何需求外封面功能。
- 下一轮建议：经人工确认后再执行下一轮；如本地浏览器策略允许，可先补做一次第 12 轮弹窗的人工视觉、文件选择预览、键盘焦点和窄屏验收。本轮立即停止，不提前执行后续轮次。

## 第 13 轮：设置界面（2026-07-19）

- 已完成：启用侧栏“设置”入口并新增仅包含需求第十一章白名单内容的 `SettingsForm` 简单表单页面。可启用或停用 Bangumi、AniList、TMDB；可用三个不重复的来源顺序设置同一作品合并时的默认资料优先级；显示各来源所需服务端环境变量名称、是否已配置和当前可用状态，但不接收或返回密钥值；可修改海报本地保存目录；可清理搜索缓存；可只读查看 SQLite 数据库位置。没有加入用户资料、头像、邮箱、密码、会员、通知、社交、提醒、推荐、统计或成就设置，也没有做成后台控制面板。
- 生效与持久化：新增 `AppSettingRepository`，只向现有 `AppSetting` 表写入 `enabled_sources`、`source_priority`、`poster_storage_path` 三个非敏感键。设置读取对缺失或损坏的本地值使用安全默认值；PATCH 以 Zod 严格校验来源集合、完整无重复的三来源优先级和目录长度。保存海报目录时解析为绝对路径，创建并验证 `default/`、`custom/` 为可写普通目录且不是符号链接；目录变化后重建导入、删除和自定义封面服务，默认/自定义海报读取路由也在每次请求时读取当前设置。外部搜索在每次请求时读取已启用来源和优先级：停用来源不会发起请求，合并主数据按用户顺序选择。清理缓存会调用三个适配器的 `clearCache`，相同关键词下一次搜索会重新请求。设置保存后失效数据源状态与外部搜索客户端缓存。
- 接口与密钥边界：新增 `GET/PATCH /api/settings`、`DELETE /api/settings/cache` 和需求指定的 `GET /api/sources`，统一使用 `{ data, error }`。`GET /api/sources` 只返回来源、标签、启用/可用布尔值以及环境变量名称、已配置布尔值、敏感标记；`BANGUMI_API_TOKEN`、`TMDB_API_KEY` 等环境变量的实际值始终只在服务端读取，不进入 `AppSetting`、客户端响应或组件属性。数据库位置只读，修改仍须通过 `DATABASE_URL` 并重启。
- 修改的关键文件：`README.md`、`src/types/settings.ts`、`src/types/api.ts`、`src/lib/validation/settings.ts`、`src/lib/sources/types.ts`、`src/lib/sources/normalize/deduplicate.ts`、`src/server/config/runtime-settings.ts`、`src/server/repositories/app-setting-repository.ts`、`src/server/services/settings-service.ts`、`src/server/services/external-search-service.ts`、`src/server/services/anime-import-service.ts`、`src/server/services/anime-delete-service.ts`、`src/server/services/anime-poster-service.ts`、`src/server/http/settings-handlers.ts`、`src/app/api/settings/route.ts`、`src/app/api/settings/cache/route.ts`、`src/app/api/sources/route.ts`、`src/app/api/posters/default/[filename]/route.ts`、`src/app/api/posters/custom/[filename]/route.ts`、`src/components/settings/SettingsForm.tsx`、`src/components/settings/SettingsForm.module.css`、`src/components/layout/AppSidebar.tsx`、`src/components/layout/AppSidebar.module.css`、`src/components/anime/AnimeLibraryHome.tsx`、`tests/settings.test.ts`、`tests/home-library-ui.test.ts`、`PROGRESS.md`。
- 执行的命令：读取 `AGENT.md`、`Project-prompts.md` 的《通用轮次协议》与《第 13 轮》、REQUIREMENTS.md 第十一章、18.2、19.5、第二十章 `GET /api/sources`、完整 `ARCHITECTURE.md`、最新进度、现有 AppSetting 模式、数据源适配器/缓存、搜索合并、海报存储和首页侧栏代码；使用 `minimalist-ui` 规范延续现有暖灰实色、编辑式标题、平面边框和无重阴影布局，并跳过与本轮冲突的动画、背景图和视觉精修建议。运行设置定向 Vitest、`npm run typecheck`、`npm run lint`、全量 `npm test`、`npm run build`。创建并迁移隔离 SQLite 与隔离海报目录，以独立端口启动开发服务并注入只用于泄漏检测的模拟密钥；真实调用设置读取、设置 PATCH、数据源状态与缓存清理接口，核对设置重新读取、目录创建、来源状态变化、响应/首页不含模拟密钥；只读检查隔离库三个设置键和四张业务表，停止服务后解析并确认目标绝对路径，再删除隔离数据库与目录。最后以只读方式复核正式数据库、正式海报目录、临时目录、客户端密钥/日志引用和禁止设置文本。
- 测试结果：通过。Next 类型生成与 TypeScript 严格检查退出码 0；ESLint 退出码 0；Vitest 14 个测试文件、106 项测试全部通过；Next.js 16.2.10 生产构建成功，新增 `/api/settings`、`/api/settings/cache`、`/api/sources` 三个动态路由。本轮新增 5 项测试，覆盖三项设置写入/重载、可写海报子目录创建和存储服务重建、重复优先级/文件路径拒绝、来源开关阻止请求、TMDB 优先级改变合并主数据、缓存命中后清理并使相同搜索再次请求、数据源状态及组件渲染不含模拟密钥、所有允许设置存在且禁止设置不存在。真实隔离 HTTP 验收的设置读取、持久化、目录创建、来源状态更新、缓存清理、API/首页密钥不泄漏 11 项判定全部为 `True`；隔离 `AppSetting` 恰有 3 个允许键，隔离四张业务表均为 0 行。客户端 `process.env` 密钥引用或含密钥变量的 `console` 语句命中 0，禁止设置文本命中 0。正式 `data/anime.db` 五张业务表均为 0 行，正式海报目录仍只有两个 `.gitkeep`，第 13 轮临时产物为 0。
- 已知问题：**浏览器运行日志未验证**。第 12 轮已确认应用内浏览器的本地 URL 策略会阻止本项目隔离端口，本轮没有重复尝试或绕过；改以真实 HTTP 响应、服务端注入模拟密钥、客户端源码/日志语句静态审计和组件静态渲染确认密钥值不进入浏览器可接收内容。第 12 轮弹窗人工视觉验收、真实 TMDB 调用、真实 `BANGUMI_USER_AGENT` 配置、第 1 轮 SWC WASM 编译回退和 6 个中等级依赖审计警告均未变化。
- 未完成内容：无（第 13 轮可执行的功能、自动化与隔离接口验证范围内）。没有把 API 密钥明文写入数据库，没有提供浏览器端密钥输入，没有新增数据库表或迁移，没有迁移旧海报文件，也没有实现用户资料、头像、通知、统计或其他禁止设置。
- 下一轮建议：经人工确认后再执行第 14 轮；若本地浏览器策略允许，可先补查第 13 轮浏览器控制台中不存在密钥值，并一并完成第 12 轮遗留的人工视觉验收。本轮立即停止，不提前实现第 14 轮。

## 第 14 轮：空状态、错误状态与可访问性（2026-07-19）

- 已完成：新增全站复用的 `ErrorState`，并把首页、详情、设置及外部搜索的读取失败统一为第十七章逐字文案“暂时无法从该数据源获取结果。”和按钮“重试”；保留并验证空库“你的动漫库还是空的。／搜索并添加第一部动漫。／添加动漫”与搜索无结果“没有找到相关动漫。／可以尝试使用其他语言名称搜索。”逐字一致。单数据源失败继续显示具体来源请求失败信息与带来源名的重试按钮，不遮挡其他来源成功结果。空库和海报失败只使用 CSS 中性轮廓或 `/placeholders/anime-poster.svg`，未加入预设、随机动漫图片或假结果。
- 可访问性完成情况：第二十三章清单逐项核对——所有按钮有可见文字或 `aria-label`，图片有动漫标题 `alt`；搜索、详情、封面管理使用命名的 `dialog`，删除确认使用命名的 `alertdialog`；搜索打开后焦点进入搜索框，详情打开后进入关闭按钮；搜索、详情、封面管理和删除确认均限制 Tab 焦点并支持 ESC；详情关闭后焦点回到原海报按钮，搜索与封面管理关闭后回到各自触发按钮；动漫卡片为原生 `button`，支持 Enter 的浏览器原生激活；全局 `:focus-visible` 覆盖按钮、输入、选择框、文本域、链接和可聚焦元素，文件选择器补充 `:focus-within`；弱文字色由不合格的 `#9a9790` 调整为 `#706d67`，文本及语义消息色对目标背景均达到 WCAG AA 4.5:1，焦点强调色达到非文本 3:1；在看/已看完、可用/不可用、已配置/未配置及数据源失败均有文字，不只靠颜色；全局加入 `prefers-reduced-motion: reduce`，统一缩短动画和过渡。模态窗口打开时其后应用区域同时设置 `inert` 和 `aria-hidden`。
- 修改的关键文件：`src/components/common/ErrorState.tsx`、`src/components/common/ErrorState.module.css`、`src/components/search/SearchEmptyState.tsx`、`src/components/search/SearchImportModal.tsx`、`src/components/search/ExternalSearchInput.tsx`、`src/components/search/SearchSourceFailures.tsx`、`src/components/anime/AnimeLibraryHome.tsx`、`src/components/anime/AnimeLibraryHome.module.css`、`src/components/anime/AnimeDetailModal.tsx`、`src/components/anime/PosterManagerModal.tsx`、`src/components/anime/PosterManagerModal.module.css`、`src/components/anime/EmptyLibraryState.tsx`、`src/components/layout/TopBar.tsx`、`src/components/settings/SettingsForm.tsx`、`src/app/globals.css`、`tests/round14-accessibility.test.tsx`、`tests/search-import-ui.test.ts`、`tests/anime-poster-management.test.ts`、`vitest.config.ts`、`package.json`、`package-lock.json`、`PROGRESS.md`。
- 执行的命令：读取 `AGENT.md`、`Project-prompts.md` 的《通用轮次协议》与《第 14 轮》、REQUIREMENTS.md 第十七章、第二十三章、9.3、第二十四章、完整 `ARCHITECTURE.md`、最新 `PROGRESS.md` 及现有状态/弹窗/海报/设置代码；扫描全站按钮、图片、dialog、过渡动画、错误文案与颜色；安装仅用于测试的 `axe-core`、`jsdom`；运行 `npm test`、`npm run lint`、`npm run typecheck`、`npm run build`；以只读方式核对正式 SQLite 五张业务表；扫描生产代码和公共资源中的随机/预设动漫图片引用。新增测试最初因测试文件匹配规则、测试路径 URL 和预期 alt 文案不准确各失败一次，均只修正本轮测试配置或断言后重新运行，最终结果如下。
- 测试结果：通过。Vitest 共 15 个测试文件、113 项测试全部通过；ESLint 退出码 0；Next 类型生成与 TypeScript 严格检查退出码 0；Next.js 16.2.10 生产构建成功。axe 在必需空态/错误态、搜索 dialog 和详情 dialog 上均报告 0 个 violation；jsdom 不实现 canvas，axe 的渲染颜色算法会输出环境提示，因此另以 WCAG 相对亮度公式自动验证 11 组全站文本/语义颜色均不低于 4.5:1、焦点色不低于 3:1。数据源失败测试实际渲染 Bangumi 失败、保留 AniList 成功状态并点击“重试 Bangumi”，回调恰执行一次。自动键盘走查记录：聚焦首页海报原生按钮并模拟 Enter 的浏览器默认激活 → 详情关闭按钮获得焦点 → 选择“已看”并确认发出 `PATCH /api/anime/1/status` → ESC 关闭 → 焦点返回原海报按钮；搜索弹窗另验证焦点进入搜索框、末尾 Tab 回到首个可聚焦控件、ESC 关闭及焦点返回“添加动漫”。正式 `data/anime.db` 的 `anime`、`franchise`、`anime_relation`、`source_reference`、`app_setting` 均为 0 行，本轮未写入假数据。
- 已知问题：**未验证：真实浏览器人工键盘走查与浏览器内 axe 颜色计算。** 第 12 轮已确认应用内浏览器对本项目本地 URL 的安全策略拦截，本轮遵守既有结论，没有重复尝试、绕过或改用其他浏览器；上述键盘链路由 jsdom 自动走查验证，结构由 axe 验证，对比度由 WCAG 公式测试验证。第 12 轮弹窗人工视觉/文件选择验收、第 13 轮浏览器运行日志、真实 TMDB 调用、真实 `BANGUMI_USER_AGENT` 配置、第 1 轮 SWC WASM 编译回退及 6 个中等级依赖审计警告保持不变。
- 未完成内容：无（第 14 轮可在当前环境完成的代码与自动化验证范围内）。第二十三章各项均有自动化、代码检查或数值验证证据；唯一无法执行的是上述受本地 URL 策略阻止的真实浏览器人工复核，已明确标注“未验证”。没有新增产品功能、数据库结构、业务接口、预设图片、随机图片或假动漫数据。
- 下一轮建议：经人工确认后再执行第 15 轮“视觉精修与响应式打磨”；若应用内浏览器本地 URL 策略解除，可先补做第 12 至 14 轮累计的人工视觉、文件选择、浏览器控制台、真实键盘和浏览器 axe 验收。本轮立即停止，不提前执行第 15 轮。

## 第 15 轮：视觉精修与响应式打磨（2026-07-19）

- 已完成：在不改数据结构、接口和业务流程的前提下完成全站视觉与三端适配。使用 `redesign-existing-projects` 规范先审计再定点升级，保留现有 Next.js、CSS Modules、暖灰色板和编辑式布局，跳过该技能中与需求冲突的背景图片、纹理、玻璃拟态、复杂动效及新增页面建议。精确保留第十三章九个基础色值：页面、侧栏、卡片、主/次文字、弱化色、边框、悬停与强调色；为兼顾第十四轮对比度，`#9A9790` 仅用于“已看完”装饰圆点，可读弱化文字继续使用已验证达到 AA 的 `#706D67`。强调色仍只用于焦点与表单选择等小面积状态，没有大面积强调色、强蓝绿状态或高饱和背景。
- 字体与视觉层级：依据 Fontsource 官方安装方式自托管 Newsreader、Noto Serif SC、Noto Sans SC 三套可变 Web Font，均由包内 `wght.css` 提供 `font-display: swap` 和 Unicode 分段，并保留中文系统衬线/无衬线回退；标题使用 Newsreader + Noto Serif SC，正文使用 Noto Sans SC，不再只依赖用户本地安装字体。标题保持 500 中等字重、平衡换行和克制字距，正文启用更适合长文的自然换行；页面继续使用实色暖中性表面、细边框、无阴影、无渐变、无纹理、无动漫背景和大量留白，海报仍是视觉中心。
- 响应式完成情况：桌面端维持 224px 侧栏、主内容最大 1500px、5 列海报；1024px 使用 184px 收缩侧栏和 4 列海报；768px 使用 184px 收缩侧栏、3 列海报并将详情改为纵向以避免窄内容区溢出；760px 以下改为真正的手机抽屉导航，带遮罩、打开/关闭标签、初始焦点、Tab 限制、ESC 关闭、焦点返回与旋转到桌面尺寸时自动解除滚动锁，海报固定 2 列。320px 主区水平留白缩至 14px；详情和封面管理弹窗距屏幕边缘仅 6px、接近全屏，封面位于资料上方；设置表单在平板窄内容区提前切换单列；主要移动控件至少 44px，卡片点击直接打开详情且不依赖 hover。
- 动效与交互：海报 hover 精确调整为 `scale(1.015)`；搜索、详情、封面管理和删除确认采用 180ms 背景淡入及 200ms `opacity 0→1 / scale 0.98→1`；所有新增过渡均在 150–220ms 范围，只使用透明度、颜色、轻微位移或缩放。按钮统一具备 180ms 状态过渡和轻微按下反馈，导航、搜索框、结果卡、错误重试、设置按钮及各模态操作补齐 hover；第十四轮的清晰 focus ring 保持不变。所有新增动画在 `prefers-reduced-motion` 下禁用或由全局规则缩短，没有弹跳、旋转、弹簧、粒子、视差、轮播、闪烁、呼吸灯或发光。
- 修改的关键文件：`src/app/layout.tsx`、`src/app/globals.css`、`src/components/layout/AppSidebar.tsx`、`src/components/layout/AppSidebar.module.css`、`src/components/layout/TopBar.module.css`、`src/components/anime/AnimeLibraryHome.module.css`、`src/components/anime/AnimeGrid.module.css`、`src/components/anime/AnimeCard.module.css`、`src/components/anime/AnimePoster.module.css`、`src/components/anime/AnimeStatusText.module.css`、`src/components/anime/AnimeDetailModal.module.css`、`src/components/anime/PosterManagerModal.module.css`、`src/components/anime/LocalSearchInput.module.css`、`src/components/anime/SortSelect.module.css`、`src/components/anime/StatusTabs.module.css`、`src/components/anime/EmptyLibraryState.module.css`、`src/components/common/ErrorState.module.css`、`src/components/modal/ConfirmDialog.module.css`、`src/components/search/ExternalSearchInput.module.css`、`src/components/search/ImportSelectionBar.module.css`、`src/components/search/SearchEmptyState.module.css`、`src/components/search/SearchImportModal.module.css`、`src/components/search/SearchResultCard.module.css`、`src/components/search/SearchSourceFailures.module.css`、`src/components/settings/SettingsForm.module.css`、`tests/round15-visual.test.ts`、`tests/round15-mobile-drawer.test.tsx`、`package.json`、`package-lock.json`、`PROGRESS.md`。
- 执行的命令：完整读取 `AGENT.md`、`Project-prompts.md` 的《通用轮次协议》与《第 15 轮》、REQUIREMENTS.md 第十二至十六章和第二十八章、完整 `ARCHITECTURE.md`、最新 `PROGRESS.md`、现有页面/组件/CSS Modules、依赖清单和最近测试；完整读取并使用 `redesign-existing-projects` 技能；查阅 Fontsource 的 Newsreader、Noto Sans SC、Noto Serif SC 官方安装页；安装三个自托管可变字体包（首次受 npm 缓存沙箱限制失败，获得授权后成功）；运行 `npm run typecheck`、`npm run lint`、全量 `npm test`、`npm run build`；扫描全部生产 CSS 的渐变、阴影、模糊玻璃、发光、粒子、视差等禁止视觉元素及全部 transition/animation；检查构建后的自托管字体资产；只读复核正式 SQLite 五张业务表。
- 测试结果：通过。Vitest 共 17 个测试文件、118 项测试全部通过；ESLint 退出码 0；Next 类型生成与 TypeScript 严格检查退出码 0；Next.js 16.2.10 生产构建成功，自托管字体进入构建产物。第 15 轮新增 5 项测试：色板九个指定色值和三套自托管字体全部存在；断点契约分别验证 320px 为抽屉/2 列/纵向详情、768px 为 184px 侧栏/3 列/纵向详情、1024px 为 184px 侧栏/4 列/左右详情、1440px 为 224px 侧栏/5 列/左右详情；主内容最大 1500px、两类移动模态近全屏规则存在；全部动画与过渡限制在 150–220ms 且 reduced-motion 存在；禁止视觉正则命中 0；主要触控尺寸不低于 44px；手机抽屉实际 DOM 验证打开、命名 dialog、初始焦点、末尾 Tab 回到首项、ESC 关闭及焦点返回。第十四轮 WCAG 对比度测试继续通过。正式 `data/anime.db` 的 `anime`、`franchise`、`anime_relation`、`source_reference`、`app_setting` 均为 0 行，本轮没有写入数据。
- 已知问题：**未验证：320 / 768 / 1024 / 1440 真实浏览器截图走查及渲染后的横向溢出检查。** 第 12 轮已确认应用内浏览器会以 URL 安全策略阻止本项目本地地址，本轮没有重复尝试、绕过或改用其他浏览器；改以四断点 CSS 契约、最小列宽/`minmax(0, 1fr)`、移动模态宽度、提前单列断点、DOM 抽屉交互和生产构建作为可执行证据。因此 hover/focus/active、reduced-motion 与对比度有代码和自动测试证据，但最终像素级外观仍待策略解除后的真实浏览器复核。第 12 轮弹窗人工视觉/文件选择验收、第 13 轮浏览器运行日志、第 14 轮真实浏览器键盘/axe 颜色计算、真实 TMDB 调用、真实 `BANGUMI_USER_AGENT` 配置、第 1 轮 SWC WASM 编译回退及 6 个中等级依赖审计警告保持不变。
- 未完成内容：无（第 15 轮可在当前环境完成的代码与自动化验证范围内）。唯一无法执行的是明确要求的真实断点截图及像素级溢出走查，已按规则标注“未验证”；没有修改数据库、迁移、API、数据源或业务逻辑，没有新增产品页面、业务功能、假数据、动漫背景、巨大横幅、渐变、阴影、玻璃拟态或禁止动画。
- 下一轮建议：应用内浏览器本地 URL 策略解除后，优先补拍 320 / 768 / 1024 / 1440 四张断点截图并检查横向溢出、实际字体回退和 hover/focus/reduced-motion；经人工确认后再执行第 16 轮“测试补全与端到端测试”。本轮立即停止，不提前执行第 16 轮。

## 第 16 轮：测试补全与端到端测试（2026-07-19）

- 已完成：逐项核对 REQUIREMENTS.md 第二十九章两份清单。14 项单元/集成要求均已有真实对应测试，新增 `tests/README.md` 覆盖矩阵，将状态约束、防重复导入、三源标准化、多来源去重、关系分类、封面优先级、上传与 URL 校验、API 错误处理、状态修改、删除、本地搜索、筛选和排序映射到具体测试文件。安装并配置 Playwright 测试运行器，使用系统现有 Microsoft Edge 实现第二十九章规定的 10 条端到端测试：空数据库首页、搜索并导入、点击海报打开详情、关闭详情、修改状态、上传替换封面、恢复默认封面、删除、外部源失败提示、阻止重复导入；10 条均逐项通过。
- E2E 隔离与可复现性：测试入口先在 `.tmp/round16-e2e` 创建一次性 SQLite 数据库和海报目录，只启用 AniList，并启动只响应固定测试契约的本地 AniList 服务；应用运行于独立端口 3130，假服务运行于独立端口 3131。所有搜索、详情、关系、默认海报下载、文件上传、状态写入和删除都通过真实 Next.js 页面/API/SQLite/文件链路完成，不访问外部网络、不读取或写入正式资料库。运行器直接管理两个服务的生命周期并在结束后删除整个隔离目录；测试产物目录加入 `.gitignore`。
- 历史“未验证”处理：第 12 轮的真实浏览器文件选择、封面预览、确认上传和恢复默认封面已由 Edge 自动化闭环验证；第 13 轮浏览器运行日志已在全部 10 条流程中检查，页面异常和非预期控制台错误为 0（浏览器自动请求需求未配置的 `/favicon.ico` 所产生的单一 404 被精确排除，没有为此新增需求外图标）；第 14 轮详情弹窗的真实浏览器初始焦点、ESC 关闭和焦点返回已验证；第 15 轮 320 / 768 / 1024 / 1440 四个真实 Edge 视口均验证 `documentElement.scrollWidth <= innerWidth`，渲染后横向溢出为 0。仍无法自动消除的项目继续保留：应用内浏览器 URL 策略仍阻止人工像素级截图/视觉复核，浏览器内 axe 颜色算法尚未执行；真实 TMDB 调用缺少 `TMDB_API_KEY`，真实 Bangumi 身份调用缺少 `BANGUMI_USER_AGENT` / Token，不能伪造凭据或结果。
- 修改的关键文件：`.gitignore`、`package.json`、`package-lock.json`、`playwright.config.ts`、`tests/README.md`、`tests/e2e/prepare.mjs`、`tests/e2e/run.mjs`、`tests/e2e/mock-anilist-server.mjs`、`tests/e2e/specs/anime-library.spec.ts`、`PROGRESS.md`。没有修改生产业务代码、数据库结构、迁移、正式数据或产品功能。
- 执行的命令：完整读取 `AGENT.md`、`Project-prompts.md` 的《通用轮次协议》与《第 16 轮》、REQUIREMENTS.md 第二十九章、完整 `ARCHITECTURE.md`、PROGRESS.md 历轮“未验证/已知问题”以及相关测试、组件、API、来源适配器和存储实现；安装 `@playwright/test`（不下载浏览器，使用系统 Edge）；运行覆盖盘点、`npm test`、`npm run typecheck`、`npm run lint`、`npm run build`、`npm run test:e2e` 及隔离 E2E 复跑；只读查询正式 SQLite 五张业务表并检查正式海报目录、临时目录和测试端口。测试运行期间曾两次因 Windows 下由 npm 间接启动的服务器进程树无法及时退出而触发外层超时，改为运行器直接管理服务后稳定正常退出；曾修正两处不精确测试期望（重复“已看完”文本定位和既有数据源失败文案），未改产品行为。
- 测试结果：通过。Vitest 17 个测试文件、118 项单元/集成测试全部通过；Playwright 在 Microsoft Edge 中运行第二十九章 10 条 E2E，10/10 通过，最终隔离复跑耗时约 11 秒；ESLint 退出码 0；Next 类型生成与 TypeScript 严格检查退出码 0；`npm run test:e2e` 的生产构建和 E2E 一体化命令退出码 0，Next.js 16.2.10 生产构建成功。正式 `data/anime.db` 的 `anime`、`franchise`、`anime_relation`、`source_reference`、`app_setting` 仍均为 0 行，正式海报目录仍只有两个既有 `.gitkeep`；`.tmp/round16-e2e` 已清理，3130/3131 无监听进程。
- 已知问题：仍未验证的项目只有需要人工或外部凭据的部分：四断点像素级截图与最终视觉观感、浏览器内 axe 颜色计算、真实 TMDB 调用、真实 Bangumi 身份调用。第 1 轮采用的 SWC WASM 编译回退继续正常工作；npm 仍报告 6 个中等级依赖审计警告，本轮未进行禁止范围外的依赖全面升级。系统 Edge 的自动化 E2E 已完成，但不等同于应用内浏览器的人工视觉验收。
- 未完成内容：无（第 16 轮范围内）。第二十九章单元/集成清单与 10 条 E2E 清单全部有对应测试且均通过；全量测试与生产构建为绿。没有借机重构业务模块、添加新功能、写入假动漫到正式库或绕过凭据和浏览器安全策略。
- 下一轮建议：经人工确认后再单独执行第 17 轮“最终功能审计与验收”；如届时具备真实 API 凭据或应用内浏览器本地 URL 策略解除，可补做上述保留的人工/外部验证。本轮立即停止，不提前执行第 17 轮。

## 第 17 轮：最终功能审计与验收（2026-07-19）

- 已完成：完整执行 REQUIREMENTS.md 第三十、三十二、三十三章最终审计。审计发现并删除顶部无点击行为且需求未列出的禁用“网格视图”按钮及其专属样式；将删除确认中的 `.note` 样式名改为 `.scope`，删除生产注释中无业务含义的 `user` 单词。真实 AniList 首次搜索“进击的巨人”仅返回主条目，暴露第 30.14 条缺口；以最小改动让每个来源搜索结果只展开数据源明确返回的一级动画关系（最多 5 个直接结果、40 个关联结果），逐关联读取完整来源详情，读取失败时保留来源关系中的最小真实资料，不检查标题、不猜测关系、不把 TMDB 用于动画关系。修复后真实 AniList 返回 11 条，覆盖 TV、OVA、SPECIAL、MOVIE、WEB。README 已更新为项目最终能力，并完整给出安装、环境配置、数据库迁移、开发/生产运行、测试和隔离 E2E 说明。
- 第三十章 30 条验收结果：
  1. **通过**：首页只组合侧栏、顶部操作、状态筛选和海报网格；空库/有数据 Edge E2E 均不存在底部详情区。
  2. **通过**：点击海报原生按钮后打开命名的独立详情 `dialog`，Edge E2E 实测通过。
  3. **通过**：ESC 关闭详情后弹窗消失、焦点返回原海报按钮，关闭前后同一卡片 `boundingBox` 完全相等，首页布局不变。
  4. **通过**：数据库 `CHECK`、TypeScript 类型、接口校验和界面选项均只允许 `WATCHING/COMPLETED`（在看/已看完）。
  5. **通过**：不存在评分功能；生产代码、数据库列、路由、文件名和组件无对应产品命中。
  6. **通过**：不存在笔记功能；审计中唯一 `.note` 样式名已改为 `.scope`。
  7. **通过**：不存在集数进度功能；仅展示数据源真实总集数。
  8. **通过**：不存在观看日期功能；数据库和界面无观看日期字段或入口。
  9. **通过**：不存在统计图表；无图表组件、页面、接口或数据库字段。
  10. **通过**：不存在用户登录；应用直接进入单用户本地动漫库。
  11. **通过**：不存在社交功能；无关注、消息、分享或社交页面/接口。
  12. **通过**：不存在推荐功能；搜索和关系只来自真实数据源结果。
  13. **通过**：隔离应用实际请求 AniList 官方 GraphQL，搜索“进击的巨人”返回 HTTP 200、来源状态 `SUCCESS`，未使用测试假响应。
  14. **通过**：修复后同一次真实搜索返回 11 条：包含 Season 2、三组 OVA、Picture Drama 特别篇、两部总集剧场版及 Chronicle 剧场版等，实际类型覆盖 TV/OVA/SPECIAL/MOVIE/WEB；页面按明确类型展示，不以标题推断。
  15. **通过**：搜索结果支持单选、多选、取消、统一状态和逐项状态；组件与批量导入集成测试通过。
  16. **通过**：导入服务按候选优先级下载并验证默认海报后写入本地；集成测试及 Edge 搜索导入流程均验证本地默认海报路径。
  17. **通过**：Edge E2E 真实选择 PNG、显示本地预览、明确确认后写入自定义目录并切换为自定义海报路由。
  18. **通过**：Edge E2E 实测恢复默认封面后自定义路径移除并重新显示默认本地海报。
  19. **通过**：首页侧栏和状态标签均支持全部、在看、已看完，使用真实数据库计数；单元、集成和 E2E 均通过。
  20. **通过**：首页只提供最近添加、标题、上映年份三种排序；中文标题及缺失年份置后由集成测试验证。
  21. **通过**：生产代码无硬编码正式动漫记录或虚假评分/资料；“进击的巨人”仅作为需求指定的搜索输入示例，正式数据库仍为空；固定动漫与响应只存在于 `tests/` 的明确测试夹具/本地 E2E 假服务。
  22. **通过**：详情、关系和元数据缺失值统一显示“暂无资料”，组件测试覆盖。
  23. **通过**：暖灰色板、自托管编辑式字体、细边框、大量留白、海报视觉中心及禁止视觉规则均由第 15 轮契约测试持续验证。
  24. **通过**：仅有动漫库、搜索覆盖层和简单设置表单，无管理后台导航、统计卡或传统 Dashboard 结构；强制词审计无产品命中。
  25. **通过**：无粉紫/蓝紫渐变、霓虹、发光、动漫背景、高饱和大色块、复杂阴影或游戏化元素；视觉契约测试通过。
  26. **通过**：系统 Edge 在 320/768/1024/1440 四个视口运行，均无横向溢出；CSS/DOM 测试覆盖手机抽屉、平板列数、桌面列数、触控尺寸和近全屏移动模态。
  27. **通过**：真实 Edge 验证详情初始焦点、ESC 关闭、焦点归还；组件测试另覆盖搜索、详情、封面和确认框的焦点限制、遮罩/ESC 与返回焦点。
  28. **通过**：Edge E2E 触发 AniList 503 后仍保持搜索弹窗，显示“AniList 请求失败。”和“重试 AniList”；多源集成测试验证其他来源继续返回。
  29. **通过**：在 `D:\Anime\.tmp\round17-clean` 创建不含 `node_modules`、`.next`、正式数据库的全新隔离副本；按 README 执行依赖安装、复制 `.env.example`、`npm run db:migrate`、`npm run dev`，本地 `http://127.0.0.1:3140` 首页和 `/api/anime` 均返回 200，首页含“我的动漫库”，API 返回空列表及 0/0/0，SQLite 完整性 `ok`、迁移 1 条、五张业务表均为 0；服务停止后隔离副本完整删除。
  30. **通过**：README 明确包含 `npm install`、`.env.local` 配置、变量说明、`npm run db:migrate`、`npm run dev`、`npm run build`、`npm start` 和 `npm run test:e2e`，并说明持久磁盘、密钥边界与隔离测试。
- 第三十二章代码输出审计：**通过**。项目为真实 Next.js/React/TypeScript 严格模式应用，不是静态概念图；Route Handler、Service、Repository、适配器、校验、数据库和文件存储分层，没有把所有逻辑放在单文件。具有统一错误响应、Zod/文件/数据库校验、Drizzle 迁移、README、`.env.example`、开发/构建/生产/测试脚本及 18 个测试文件。生产 `src/`、`drizzle/`、`public/` 中 TODO/FIXME/HACK/XXX 为 0；不可点击的装饰按钮已删除，剩余禁用态均由未输入、提交中或已导入等真实交互条件控制；生产代码无硬编码正式动漫数据、虚假 API 响应或写死密钥。测试令牌、动漫夹具和本地 AniList 假服务严格位于 `tests/`，不进入生产运行。
- 第三十三章强制关键词审计：**通过，零越界遗留**。对 `src/`、`drizzle/`、`public/` 和文件名执行大小写不敏感的整词审计。产品字段、按钮、页面、组件和文件名中的禁词命中为 0；仅保留 5 个必要且已解释的非产品命中：AniList 官方 GraphQL 字段 `startDate` 两处（通过 `release` 别名后只归一化为 `year`）、Bangumi 官方 `User-Agent` 请求头/配置提示两处、Drizzle 自动生成迁移日志 `_journal.json` 的内部 `tag` 键一处。REQUIREMENTS/FEATURE-BOUNDARIES/PROGRESS 中的禁词属于需求与审计记录；`tests/` 中的禁词、固定条目和假响应用于证明禁止项不存在或隔离测试，不是产品产物。新增 `round17-final-audit.test.ts` 固化允许命中清单、禁止文件名、未完成标记、假网格按钮和 README 必备步骤，4 项全部通过。
- 修改的关键文件：`README.md`、`src/components/layout/TopBar.tsx`、`src/components/layout/TopBar.module.css`、`src/components/modal/ConfirmDialog.tsx`、`src/components/modal/ConfirmDialog.module.css`、`src/components/anime/PosterPreview.tsx`、`src/lib/sources/types.ts`、`src/lib/sources/anilist/adapter.ts`、`src/lib/sources/anilist/schemas.ts`、`src/lib/sources/normalize/anilist.ts`、`src/server/services/external-search-service.ts`、AniList 测试夹具、`tests/multi-source-search.test.ts`、`tests/e2e/mock-anilist-server.mjs`、`tests/e2e/specs/anime-library.spec.ts`、`tests/round17-final-audit.test.ts`、`PROGRESS.md`。没有修改数据库结构或迁移。
- 执行的命令：完整读取本轮协议、REQUIREMENTS.md 第三十/三十二/三十三章、FEATURE-BOUNDARIES.md、ARCHITECTURE.md、README.md、全部 PROGRESS.md、相关生产代码和最近测试；对全库和生产目录执行禁词、文件名、按钮、禁用态、TODO/FIXME、硬编码动漫、测试假响应和密钥模式检索；运行定向适配器/多源/导入测试；两次在隔离服务中真实请求 AniList“进击的巨人”并对比修复前后结果；在全新隔离副本中先尝试离线安装（因缓存缺少 `xmlchars` 真实失败），随后按 README 正常 `npm install` 成功安装 486 个包，迁移并启动；运行 `npm run db:check`、`npm run typecheck`、`npm run lint`、全量 `npm test`、`npm run test:e2e` 和 E2E 复跑；只读检查正式 SQLite 完整性、外键、列、五表计数、正式海报目录、临时目录和测试端口。
- 测试结果：通过。Vitest 18 个文件、123 项测试全部通过；新增 1 项明确关系扩展测试和 4 项最终审计测试。ESLint 退出码 0；Next 路由类型生成和 TypeScript 严格检查退出码 0；Next.js 16.2.10 生产构建成功；最终 Microsoft Edge E2E 10/10 通过，包含搜索关联结果后的精确主条目选择、详情关闭前后布局一致、状态、封面、删除、错误隔离和重复导入。真实 AniList 搜索 HTTP 200、来源 `SUCCESS`、11 条结果。Drizzle 迁移一致性通过；正式 SQLite 完整性 `ok`、外键违规 0、五张业务表均为 0 行；正式海报目录只有两个既有 `.gitkeep`；隔离数据库、依赖副本、日志和端口均已清理。
- 已知问题：不影响第三十章验收的环境限制仍保留说明：当前没有用户提供的真实 `TMDB_API_KEY`、`BANGUMI_USER_AGENT` / Token，因此本轮真实外部验收使用无需密钥的 AniList；系统 Edge 自动化已验证关键交互和四断点无溢出，但应用内浏览器 URL 策略仍阻止人工像素级截图/浏览器内 axe 颜色计算。Windows 继续使用已验证的 SWC WASM 回退。npm 审计仍报告 6 个中等级依赖告警，自动修复会涉及禁止范围外的破坏性大版本变化，未执行。
- 未完成内容：无。第三十章 30 条全部标记“通过”；第三十二章代码输出要求通过；第三十三章无越界产品遗留，所有必要技术命中均逐项解释；README 从零启动成功；全量测试、E2E、类型检查、Lint、迁移检查和生产构建均为绿。项目按当前需求完成交付。
- 下一轮建议：无后续实现轮次；项目已完成第 17 轮最终审计与验收。若未来提供真实 TMDB/Bangumi 凭据或解除应用内浏览器本地 URL 策略，可作为独立验证任务补充人工/外部证据，不应在未修改原始需求的情况下继续添加功能。本轮立即停止。
