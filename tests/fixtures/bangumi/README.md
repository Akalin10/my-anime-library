# Bangumi response fixtures

These fixtures are development-only test data. They were captured from the current Bangumi v0 API on 2026-07-19 using the public search, subject detail, and subject relations endpoints for subject `55770`.

Only fields consumed by the adapter are retained. Unrelated fields, including ratings, rankings, collection counts, and tags, were deliberately removed. Production code never loads these files.
