# AniList fixtures

These reduced fixtures preserve the official GraphQL response shape returned by
`https://graphql.anilist.co` for public `Media` queries for AniList anime ID 1
on 2026-07-19. Long descriptions and unrelated relation edges were omitted to
keep the fixtures focused; identifiers, titles, dates, formats, studios, cover
URLs and the retained relation values come from the real response.
